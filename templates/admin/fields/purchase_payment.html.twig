{%  set target =  field.customOptions.get('target') %}
{%  set payment =  field.value %}

{% if payment.payer is defined %}
    {% if target == 'payer_name' %}
        {{ payment.payer.name.given_name ~ ' ' ~ payment.payer.name.surname }}
    {% elseif target == 'payer_email' %}
        {{ payment.payer.email_address }}
    {% elseif target == 'shipping_name' %}
        {{ payment.purchase_units[0].shipping.name.full_name }}
    {% elseif target == 'shipping_address' %}
        {%  set address =  payment.purchase_units[0].shipping.address %}
        <address>
            {{ address.address_line_1 }}
            <br>
            {{ address.postal_code }}
            {{ address.admin_area_1 == '' or address.admin_area_1 == 'Empty' ? '' :  address.admin_area_1 }}
            {{ address.admin_area_2 }}
            <br>
            {{ address.country_code }}
        </address>
    {% endif %}
{% endif %}
